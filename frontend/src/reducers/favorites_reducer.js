// import { ADD_INTERNSHIP_LIKE, REMOVE_INTERNSHIP_LIKE } from "../actions/types";

// export default function favoritesReducer(state = [], action) {
//   switch (action.type) {
//     case ADD_INTERNSHIP_LIKE:
//       return [...state, action.payload];
//     case REMOVE_INTERNSHIP_LIKE:
//       return state.filter((e) => {
//         if (e.name !== action.payload.name) {
//           return true;
//         }
//         return false;
//       });
//     default:
//       return state;
//   }
// }
